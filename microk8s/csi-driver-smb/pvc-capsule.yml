apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-smb-capsule
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: smb-capsule
  volumeName: pv-smb-capsule
